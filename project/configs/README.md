# Конфигурация

- `.env.example` — пример переменных окружения без секретов.
- `config.yaml` — человекочитаемое описание основных путей, endpoint-параметров и целевых переменных.

Реальный `.env` не должен попадать в Git. Он уже игнорируется в корневом `.gitignore`.
